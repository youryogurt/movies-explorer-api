{
  "extends": "airbnb-base",
  "rules": {
    "no-underscore-dangle": "off"
  },
  "overrides": [
    {
      "files": ["*.js"],
      "rules": {
        "no-underscore-dangle": ["error", { "allow": ["_id"] }]
      }
    }
  ]
}